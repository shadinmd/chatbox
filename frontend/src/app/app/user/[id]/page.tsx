"use client";
import { useEffect } from "react";

const User = ({ params }: { params: { id: string } }) => {

	useEffect(() => {
		
	}, [])

	return (
		<div>
			User
		</div>
	)
}

export default User
